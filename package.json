{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "dev": "concurrently -n web,api -c cyan,yellow 'pnpm -F hubworkz dev' 'pnpm -F api-server dev'",
    "dev:gestor": "pnpm -F gestor-app dev",
    "dev:paciente": "pnpm -F paciente-app dev",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "build:mobile": "pnpm -F gestor-app build && pnpm -F paciente-app build",
    "start": "node artifacts/api-server/dist/index.mjs",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "concurrently": "^10.0.0",
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  },
  "dependencies": {
    "xlsx": "^0.18.5"
  }
}
